# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# 本地词典数据（ECDICT stardict.db，约 850MB，见 README「离线词典」；不随仓库分发）
# 用 /data/ 锚定仓库根目录，避免误伤 src/data/
/data/
/LingKuma/

NOTICE.md
DEPLOY.md

node_modules
dist
dist-ssr
*.local

# 构建期环境变量（VITE_API_BASE / BASE_PATH）。刻意不提供 .env 模板：这两个值只该在
# 命令行前缀或 CI 里设。Vite 对每个命令都会加载 .env*，包括 npm test —— 在里面设
# VITE_API_BASE 会让 src/__tests__ 里「后端请求必须是同源相对路径」的断言失败。
# .env.local 由上面的 *.local 覆盖。
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

